
// Find Maximum and Minimum: Write functions to find the maximum and minimum values in an array of numbers.

// function findMaximum(arr) {
//     if (arr.length === 0) return null;
//     return Math.max(...arr);
// }

// function findMinimum(arr) {
//     if (arr.length === 0) return null;
//     return Math.min(...arr);
// }

// let numbers = [330, 130, 44, 134, 50, 94, 23, 61, 45];
// let maximum = findMaximum(numbers);
// let minimum = findMinimum(numbers);
// console.log(`Maximum: ${maximum}, Minimum: ${minimum}`); 


// Sum of Array: Create a function that calculates the sum of all elements in an array

// function sumArray(arr) {
//     return arr.reduce((acc, curr) => acc + curr, 0);
// }
// let numbers = [15, 11, 64, 54];
// let totalSum = sumArray(numbers);
// console.log(`${totalSum}`);


// Filter Array: Implement a function that filters out elements from an array based on a given condition.

// function filterArray(arr, conditionFunc) {
//     return arr.filter(conditionFunc);
// }
// function isEven(num) {
//     return num % 2 === 0;
// }

// let numbers = [17, 80, 101, 20, 300, 409, ];
// let filteredNumbers = filterArray(numbers, isEven);
// console.log(filteredNumbers);
